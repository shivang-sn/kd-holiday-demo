Drop real photography here once the client supplies it (office, team, tour groups,
destinations, events, customer photos - see docs/website-build-brief.md Section 5).

Until then, the site renders CSS gradient placeholders (.placeholder-img in
src/css/base.css) labeled with what each image should be, so every template is
ready to receive real files without markup changes.
